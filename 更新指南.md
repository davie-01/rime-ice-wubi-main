# 上游更新操作指南

雾凇拼音 / 极点五笔 发新版时怎么升级，以及本方案改过哪些东西。

---

## 一、目录结构说明

图例：**雾** = 来自雾凇拼音　**笔** = 来自极点五笔　**改** = 本方案改过　**新** = 本方案新增

```
整合方案/
│
│ ── 全局配置 ─────────────────────────────────────────────────────────
├── default.yaml                     雾 改  全局配置：方案列表、标点、快捷键、开关记忆
├── squirrel.yaml                    雾 改  鼠须管（macOS）界面：皮肤、字体、app_options
├── weasel.yaml                      雾      小狼毫（Windows）界面，macOS 不读，留作双平台通用
│
│ ── 雾凇拼音 ─────────────────────────────────────────────────────────
├── rime_ice.schema.yaml             雾      方案 - 雾凇拼音（全拼）
├── rime_ice.dict.yaml               雾      词库入口，挂载 cn_dicts/ 下的各分词库
├── double_pinyin_flypy.schema.yaml  雾 改  方案 - 小鹤双拼（已改为全拼混输 + 小鹤码提示）
│
├── melt_eng.schema.yaml             雾      方案 - 英文输入，作为次翻译器挂在拼音方案里
├── melt_eng.dict.yaml               雾      英文词库入口，挂载 en_dicts/
├── radical_pinyin.schema.yaml       雾      方案 - 部件拆字，供反查与辅码使用
├── radical_pinyin.dict.yaml         雾      部件拆字词库（2.1 MB）
│
├── symbols_v.yaml                   雾      符号表 - 全拼的 v 模式（输入 v 后接符号）
├── symbols_caps_v.yaml              雾      符号表 - 双拼的 V 模式（双拼里 v 是声母，改用大写 V）
│
├── custom_phrase.txt                雾      自定义短语 - 全拼用，优先级高于词库
├── custom_phrase_double.txt         新      自定义短语 - 小鹤双拼用（上游需手动创建，这里给了空文件）
│
│ ── 极点五笔 ─────────────────────────────────────────────────────────
├── wubi86_jidian.schema.yaml        笔 改  方案 - 极点五笔86（主力方案）
├── wubi86_jidian_trad.schema.yaml   笔 改  方案 - 简入繁出（未入方案列表，改用「简体→繁体」开关）
├── wubi86_jidian.dict.yaml          笔 改  五笔主码表（1.3 MB），import 其余扩展词库
├── wubi86_jidian_extra.dict.yaml    笔      扩展词库：汽车、影视、软件名、医学、三四叠字等 30 余个分组
├── wubi86_jidian_extra_district...  笔      扩展词库 - 全国行政区划名（默认未启用）
├── wubi86_jidian_user.dict.yaml     笔      个人私有词库，自己造的词写这里 ★更新时勿覆盖
├── wubi86_jidian.ico                笔      Windows 任务栏图标，macOS 不读
│
├── pinyin_simp.schema.yaml          笔 改  方案 - 简体拼音，仅作五笔 z 键反查的码表，不入方案列表
├── pinyin_simp.dict.yaml            笔      简体拼音码表（2 MB），同上
│
├── numbers.schema.yaml              笔      方案 - 大写数字（壹贰叁 / 一二三）
│
│ ── 词库目录 ─────────────────────────────────────────────────────────
├── cn_dicts/                        雾      中文词库（45 MB），由 rime_ice.dict.yaml 挂载
│   ├── 8105.dict.yaml                       通用规范汉字表 8105 字
│   ├── 41448.dict.yaml                      大字表（默认未启用，生僻字需要时开）
│   ├── base.dict.yaml                       基础词库
│   ├── ext.dict.yaml                        扩展词库
│   ├── tencent.dict.yaml                    腾讯词向量大词库（部署耗时主要来自这个）
│   └── others.dict.yaml                     杂项 + 错音错字容错词
│
├── en_dicts/                        雾      英文词库（448 KB）
│   ├── en.dict.yaml                         英文主词库
│   ├── en_ext.dict.yaml                     英文补充词库
│   ├── cn_en.txt                            中英混输词条 - 全拼版
│   └── cn_en_flypy.txt                      中英混输词条 - 小鹤双拼版
│
├── opencc/                          雾      OpenCC 转换配置（184 KB）
│   ├── emoji.json                           Emoji 转换规则入口
│   ├── emoji.txt                            词 → Emoji 映射表
│   └── others.txt                           其他转换映射
│
│ ── Lua 脚本 ─────────────────────────────────────────────────────────
├── lua/                             雾      方案里 lua_translator / lua_filter 引用的脚本（880 KB）
│   ├── date_translator.lua                  日期时间星期（rq / sj / xq / dt / ts）
│   ├── lunar.lua + lunar.db                 农历（nl）
│   ├── number_translator.lua                数字金额大写（R 前缀）
│   ├── calc_translator.lua                  计算器（cC 前缀）
│   ├── unicode.lua                          Unicode 字符（U 前缀）
│   ├── uuid.lua                             生成 UUID
│   ├── corrector.lua                        错音错字提示（全拼在用；双拼已关，让位给编码提示）
│   ├── search.lua                           辅码筛选（` 引导）
│   ├── select_character.lua                 以词定字（[ ]）
│   ├── v_filter.lua                         v 模式符号优先
│   ├── long_word_filter.lua                 长词优先
│   ├── pin_cand_filter.lua                  置顶候选项
│   ├── reduce_english_filter.lua            降低部分英文词的候选位置
│   ├── autocap_filter.lua                   英文自动大写
│   ├── force_gc.lua                         内存回收
│   ├── cold_word_drop/                      降低生僻词词频的组件（默认未启用）
│   └── 其余                                 未被当前方案引用，保留以备启用
│
│ ── 文档与仓库文件 ───────────────────────────────────────────────────
├── README.md                        新      仓库首页：特性、部署方法、来源与许可
├── 更新指南.md                       新      本文件：目录结构、上游更新、git 操作、报错速查
├── 说明.md                          新      本地笔记，不纳入 git（见 .gitignore）
├── LICENSE                          雾      GPL-3.0，本仓库整体分发许可
├── licenses/                                两个上游项目的完整许可证原文
│   ├── GPL-3.0-rime-ice.txt
│   └── Apache-2.0-rime-wubi86-jidian.txt
└── .gitignore                       新      排除 build/、*.userdb/、installation.yaml 等
```

> ★ 标记的文件承载用户数据，更新时务必保留。
> 另有三类文件会在部署后自动生成，**不在本目录内，也不要手工创建**：
> `build/`（编译产物）、`*.userdb/`（动态词频）、`installation.yaml`（同步标识）。

---

## 二、本方案相对上游改过的文件（共 8 个）

其余文件（`lua/`、`opencc/`、`cn_dicts/`、`en_dicts/`、`symbols_*.yaml`、
`rime_ice.schema.yaml`、`melt_eng.*`、`radical_pinyin.*`、各 `.dict.yaml` 等）
**与上游完全一致，更新时整体覆盖即可**。

| 文件 | 来源 | 改动内容 | 能否改用 `.custom.yaml` |
|---|---|---|---|
| `default.yaml` | 雾凇 | schema_list、config_version、save_options | ✅ 推荐迁移 |
| `squirrel.yaml` | 雾凇 | 注释绿色、app_options、横排布局 | ✅ 推荐迁移 |
| `wubi86_jidian.schema.yaml` | 五笔 | 键位、zh_trad、用户词典、依赖、反查提示 | ✅ 推荐迁移 |
| `wubi86_jidian_trad.schema.yaml` | 五笔 | 同上 | ✅ 推荐迁移 |
| `double_pinyin_flypy.schema.yaml` | 雾凇 | algebra 混输、编码提示、关 corrector | ⚠️ 可迁但收益有限 |
| `pinyin_simp.schema.yaml` | 五笔 | 去掉 stroke 依赖和多余节点 | ⚠️ patch 不擅长删节点 |
| `wubi86_jidian.dict.yaml` | 五笔 | 去掉 hamster 词库 import（1 行） | ❌ 词库无 patch 机制 |
| `custom_phrase_double.txt` | 新增 | 小鹤双拼自定义短语（空） | — 新增文件，不涉及 |

---

## 三、更新步骤

```sh
# 0. 备份整个配置目录
cp -R ~/Library/Rime ~/Desktop/Rime-backup-$(date +%Y%m%d)

# 1. 下载上游新版，解压到 srf/ 下（替换 rime-ice-main / rime-wubi86-jidian-master）

# 2. 在 整合方案/ 里逐一覆盖「未改动」的文件和目录
#    lua/ opencc/ cn_dicts/ en_dicts/ symbols_*.yaml rime_ice.* melt_eng.* radical_pinyin.*
#    以及各 .dict.yaml（wubi86_jidian.dict.yaml 除外，见下）

# 3. 对照上表，把 8 个改过的文件逐一重做（或确认 .custom.yaml 仍然生效）

# 4. 删掉旧的编译产物
rm -rf ~/Library/Rime/build

# 5. 拷贝到 ~/Library/Rime/，重新部署
```

### 用 diff 核对改动（关键一步）

保留 `rime-ice-main/` 和 `rime-wubi86-jidian-master/` 这两个**原始上游目录**，
更新后跑一遍比对，一眼就能看出哪些文件被自己改过、改了多少行：

```sh
cd ~/Documents/Claude/work/srf
for f in 整合方案/*.yaml 整合方案/*.txt; do
  b=$(basename "$f")
  up=""
  [ -f "rime-ice-main/$b" ] && up="rime-ice-main/$b"
  [ -f "rime-wubi86-jidian-master/$b" ] && up="rime-wubi86-jidian-master/$b"
  if [ -n "$up" ]; then
    diff -q "$up" "$f" >/dev/null 2>&1 || echo "改过  $b"
  else
    echo "新增  $b"
  fi
done
```

---

## 四、必须注意的坑

**1. `.custom.yaml` 优先级高于同名 `.yaml`**
这正是当初两个包互相覆盖的原因：五笔包的 `default.custom.yaml` 把雾凇的 `schema_list` 整个顶掉了。
**自己的补丁只能有一套**，部署前务必确认 `~/Library/Rime/` 下没有来路不明的 `*.custom.yaml`。

**2. `default.yaml` 的 `config_version` 必须大于程序目录同名文件**
否则整个文件不生效。改用 `default.custom.yaml` 则没有这个限制（patch 不看版本号）。

**3. 补丁里的「列表」是整体替换，不是合并**
`patch: speller/algebra: [...]` 会把上游那份完整替换掉。
所以上游若改进了 algebra，你的补丁会静默盖掉它 —— 更新后仍需人工核对列表类改动。
标量（如 `translator/enable_user_dict: true`）没有这个问题，是补丁最划算的场景。

**4. 词库文件（`.dict.yaml`）没有 patch 机制**
`wubi86_jidian.dict.yaml` 删掉 hamster import 那一行，每次更新都得手工重做。
（也可以反过来：保留上游原样，把空的 `wubi86_jidian_user_hamster.dict.yaml` 建回来。）

**5. 这些用户数据不能被覆盖**
- `*.userdb/` —— 动态词频，整个目录拷贝，别只拷里面的文件
- `installation.yaml` —— 同步标识
- `custom_phrase.txt` / `custom_phrase_double.txt` —— 自定义短语
- `wubi86_jidian_user.dict.yaml` —— 五笔个人词库

**6. 更新后重点核对**
- `default.yaml` 的 `schema_list` 里四个方案是否都还在
- `lua/` 下的脚本文件名有没有变（方案里 `lua_filter@*xxx` 是按文件名引用的）
- 五笔的编码提示、双拼的小鹤码提示是否还正常显示

---

## 五、补丁写法参考

上游自带范例：`rime-ice-main/others/patch_examples/`

```yaml
# default.custom.yaml
patch:
  schema_list:
    - schema: rime_ice
    - schema: double_pinyin_flypy
    - schema: wubi86_jidian
    - schema: numbers
  "switcher/save_options":
    - ascii_punct
    - traditionalization
    - zh_trad
    - emoji
    - search_single_char
```

```yaml
# wubi86_jidian.custom.yaml
patch:
  "translator/enable_user_dict": true
  "translator/db_class": userdb
```

```yaml
# squirrel.custom.yaml
patch:
  "style/candidate_list_layout": linear
  "preset_color_schemes/purity_of_form_custom/comment_text_color": 0x5AC461
  "preset_color_schemes/purity_of_form_custom/hilited_comment_text_color": 0x5AC461
  app_options:
    com.apple.Terminal:
      ascii_mode: true
```

---

## 六、Git 日常操作

远程仓库：`https://github.com/davie-01/rime-ice-wubi-main`
本地路径：`~/Documents/Claude/work/srf/整合方案`

> git 命令建议在 **macOS 自己的终端**里跑，权限完整、不会卡在锁文件上。

### 提交历史的结构

```
main
├── 本方案定制 …            ← 后续提交都在这上面
└── 上游原版      tag: upstream-baseline
```

`upstream-baseline` 这个标签指向**未经修改的上游文件**，是整套配置的参照系。

### 改完配置后

```sh
cd ~/Documents/Claude/work/srf/整合方案
git status                     # 先看改了哪些
git diff                       # 再看具体改了什么
git add -A
git commit -m "简短说明改了什么"
git push
```

### 查看本方案相对上游的全部差异

```sh
git diff upstream-baseline HEAD           # 完整 diff
git diff --stat upstream-baseline HEAD    # 只看哪些文件、多少行
```

修改上游文件后，记得同步更新 README「已作的修改」一节 —— GPL-3.0 和 Apache-2.0 都要求标明改动。

### 换机器 / 重装系统

```sh
git clone https://github.com/davie-01/rime-ice-wubi-main.git
cd rime-ice-wubi-main
```

⚠️ **词频不会跟着过来**。`*.userdb/` 在 `.gitignore` 里（LevelDB 二进制，多机同步会损坏）。
要带走的话，手动拷贝原机器上的 `~/Library/Rime/rime_ice.userdb/` 和 `wubi86_jidian.userdb/`
两个**整个目录**，别走 git。

### 部署到输入法目录

```sh
rsync -av --delete \
  --exclude='.git' --exclude='.gitignore' --exclude='*.md' \
  --exclude='LICENSE' --exclude='licenses' \
  --exclude='*.userdb' --exclude='installation.yaml' --exclude='build' \
  ~/Documents/Claude/work/srf/整合方案/ ~/Library/Rime/
```

然后鼠须管 →「重新部署」。

`--delete` 会清掉目标目录里多余的文件（比如上游删掉的旧方案），但 `--exclude` 保住了
用户数据和编译产物。**首次使用前务必先备份 `~/Library/Rime/`**，确认无误再常态化使用。

### 移动仓库文件夹

整个文件夹搬走即可，`.git` 记的都是相对路径，换目录、换盘、换机器都不受影响。

- 必须连隐藏的 `.git` 一起搬 —— 用 `mv 整合方案 新位置/`，**别用 `cp -R 整合方案/* 新位置/`**，通配符不匹配点开头的文件
- 别放进 iCloud「桌面与文稿」同步目录，`.git` 里大量小文件会被反复同步，可能损坏仓库
- 别在 `~/Library/Rime/` 里建仓库 —— 那是部署目录，输入法会往里写 `build/`、`*.userdb/`

---

## 七、GitHub 认证与 Token

HTTPS 方式推送时 GitHub **不接受账号密码**，必须用 Token 或 OAuth。

### 方式一：gh CLI（推荐，不用管 Token）

```sh
brew install gh
gh auth login
```

依次选：**GitHub.com** → **HTTPS** → 「Authenticate Git with your GitHub credentials?」选 **Yes**
→ **Login with a web browser** → 复制终端给的 8 位码 → 浏览器里粘贴并授权。

之后 git 自动使用它的凭据，不用再输密码，也不用操心 Token 过期。

### 方式二：Classic Token

**菜单路径**（层级较深）：

```
GitHub 右上角头像
  └ Settings
      └ 左侧栏拉到最底部：Developer settings
          └ Personal access tokens
              └ Tokens (classic)          ← 注意选 classic
                  └ Generate new token (classic)
```

填写要点：

| 项 | 说明 |
|---|---|
| Note | 随便写，用于日后辨认，如 `mac-rime` |
| Expiration | 到期时间。选 No expiration 则永不过期（方便但风险高） |
| Select scopes | **把 `repo` 这一整块勾上**（点父项 `repo`，子项会全选） |

生成后的 Token **只显示这一次**，立刻复制存好。

推送时：用户名填 `davie-01`，**密码位置粘贴 Token**（终端里粘贴不显示字符，属正常）。

### 方式三：Fine-grained Token（坑较多，不推荐）

路径同上，但选 `Fine-grained tokens`。需要额外注意**两处**，少一处就是 403：

1. **Repository access** —— 必须包含目标仓库。选了「Only select repositories」时，
   新建的仓库不会自动加进已有 Token，要手动添加
2. **Permissions → Contents** —— 必须改成 **Read and write**，默认只有 Read

### 换了 Token 之后必须清钥匙串

这一步最容易漏。网页上改了权限或换了新 Token，macOS 钥匙串里还存着旧的，
git 会继续拿旧凭据去试，于是一直 403：

```sh
printf "protocol=https\nhost=github.com\n\n" | git credential-osxkeychain erase
```

清完再 push，会重新提示输入用户名和密码。

### 验证 Token 是否真的有写权限

```sh
gh api repos/davie-01/rime-ice-wubi-main --jq .permissions
```

正常返回 `{"admin":true,"push":true,"pull":true}`，`push` 为 false 就是权限没给对。

---

## 八、报错速查

| 报错 | 真正原因 | 处理 |
|---|---|---|
| `Permission to xxx denied to davie-01` + 403 | 身份认证**通过了**，是 Token **权限**不够（不是账号错） | 重新生成 classic token 勾全 `repo` → 清钥匙串 → 重推 |
| `! [rejected] main -> main (fetch first)` | 远端有本地没有的提交，常见于在 GitHub 网页上改过文件 | `git pull --rebase origin main` 后再 push |
| push 输出只有 `[new tag]`，没有 `[new branch]` | 分支根本没推上去，Code 页面因此是空的 | `git push -u origin main` |
| GitHub 弹出 `Confirm access` / sudo mode | 敏感操作前的二次验证 | 输入验证器 App 的 6 位码；有 Passkey 可点输入框右侧盾牌 |
| `cannot lock ref 'HEAD'` / `HEAD.lock: File exists` | 上一个 git 进程异常中断，残留锁文件 | `rm -f .git/HEAD.lock .git/index.lock` 后重试 |
| rebase 冲突时 `--theirs` / `--ours` 分不清 | rebase 期间语义是反的 | **rebase 时 `--theirs` = 你自己的版本** |
| `fatal: no rebase in progress` | 冲突解决命令只能在 rebase **进行中**用 | 先 `git pull --rebase origin main`，冲突出现后再解决 |

### 实在乱了

```sh
git rebase --abort          # 放弃 rebase，回到操作前
git reset --hard HEAD       # 丢弃所有未提交改动（谨慎）
git reflog                  # 查看所有历史操作，可据此找回"丢失"的提交
```

`git reflog` 是最后的保险 —— 只要提交过，基本都能找回来。
